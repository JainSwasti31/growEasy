# URL where the server API is reachable during local development
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional: if your client uses feature flags or telemetry keys, add them here
# NEXT_PUBLIC_FEATURE_X=true

# Security note: keep secrets out of this file in git; use .env.local for overrides
# URL of the Express backend
NEXT_PUBLIC_API_URL=http://localhost:4000
